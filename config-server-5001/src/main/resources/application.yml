server: 
  port: 5001
  
spring:
  application:
    name:  config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:SuperCNJ/springcloud-demo-config.git #GitHub上面的git仓库名称
 

